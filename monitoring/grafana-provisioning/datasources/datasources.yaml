apiVersion: 1

datasources:
  - name: InfluxDB
    type: influxdb
    access: proxy
    isDefault: true
    url: http://influxdb:8086
    database: k6
    user: admin
    password: admin
    jsonData:
      httpMode: POST
